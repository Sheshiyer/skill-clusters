interface:
  display_name: "Skill Clusters"
  short_description: "Repo-specific patterns and workflows for skill-clusters"
  default_prompt: "Use the skill-clusters repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true